package pivot

// writer.go is intentionally thin — the writer type lives in pivot.go
// alongside NewWriter so that the full implementation is in one place.
// This file exists as a placeholder to keep the package layout consistent
// with the rest of the project (e.g. filter, transform, annotate).
